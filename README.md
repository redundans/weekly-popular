# Weekly Popular

The forum API uses the alias `weekly-popular` for the server-side sort.

Example:

```text
/api/discussions?sort=weekly-popular
```

Do not use `sort=-weeklyPopular`; the alias already represents descending order.
