{
  "name": "@redundans/weekly-popular",
  "version": "0.1.0",
  "private": true,
  "description": "JavaScript assets for the Flarum Weekly Popular extension.",
  "scripts": {
    "dev": "webpack --mode development --watch",
    "build": "webpack --mode production",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write src *.ts",
    "format-check": "prettier --check src *.ts"
  },
  "devDependencies": {
    "flarum-webpack-config": "^2.0.2",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.0"
  }
}
